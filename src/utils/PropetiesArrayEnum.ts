// the order is very important
export enum PropetiesArrayEnum {
  url,
  address,
  district,
  description,
  min_value,
  avaliation_value,
  discount,
  type_selling,
  photo_url,
  city,
  state,
}

console.log(PropetiesArrayEnum[1]);
